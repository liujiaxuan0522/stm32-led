# STM32 Blue Pill 流水灯 —— Keil 完整操作教程

> 从装软件到看灯亮，每一步都写清楚。

---

## 〇、总览：干活的顺序

```
装软件  →  接线  →  建工程  →  改配置  →  编译  →  烧录  →  看现象
```

前两步做一次就行，后面每次改代码只要重复「编译 → 烧录」。

---

## 一、准备软件（只做一次）

需要装 3 样东西：

| 软件 | 说明 | 去哪下 |
| --- | --- | --- |
| **Keil MDK-ARM 5.x** | 主程序，写代码和编译 | keil.com（官网，需注册账号） |
| **STM32F1xx_DFP** | STM32F1 系列的器件支持包 | keil.com/dd2/pack/ 搜 STM32F1 |
| **ST-Link 驱动** | 让电脑认出 ST-Link 下载器 | ST 官网，或跟着 Keil 提示装 |

### 安装顺序

1. 先装 **Keil MDK**，一路下一步。安装路径**不要带中文和空格**（比如 `D:\Keil_v5`），否则后面容易出各种怪问题。
2. 双击 **`Keil.STM32F1xx_DFP.x.x.x.pack`**，它会自动找到 Keil 并安装。
3. **ST-Link 驱动** —— 见下面 1.1 节，这一步单独讲，因为卡住的人最多。

### 1.1 ST-Link 驱动怎么装

#### 第一步：插上先看电脑认不认

把 ST-Link 插到电脑 USB 口，按 **Win + X** → 打开「设备管理器」，看：

| 情况 | 说明 | 下一步 |
| --- | --- | --- |
| 「通用串行总线设备」下有 `STMicroelectronics STLink dongle` | 驱动已装好 | ✅ 跳过，不用折腾 |
| 「其他设备」下有**带黄色感叹号**的 `STLink dongle` / 未知设备 | 认到硬件了，缺驱动 | 继续第二步 |
| **哪里都找不到** | 不是驱动问题，是线或口的问题 | 换主板**后置 USB 口**、换一根**能传数据**的线 |

#### 第二步：装驱动（三种方法，从省事到可靠）

**方法 A：让 Windows 自动装（先试这个）**

设备管理器里**右键那个带感叹号的设备** → **更新驱动程序** → **自动搜索驱动程序**。

Windows 10/11 的驱动库里带 ST-Link 驱动，很多时候直接就装上了。

**方法 B：从 ST 官网下官方驱动（最可靠）**

1. 浏览器搜 **`STSW-LINK009`**，进 ST 官网页面（st.com）
2. 下载 `en.stsw-link009.zip`（需要填邮箱，随便填）
3. 解压，里面有两个安装程序：

   ```
   dpinst_amd64.exe   ← 64 位系统用这个（绝大多数电脑）
   dpinst_x86.exe     ← 32 位系统用这个
   ```

4. **右键 → 以管理员身份运行** `dpinst_amd64.exe`
5. 一路 Next，装完重启电脑

> 不确定自己是几位系统？右键「此电脑」→ 属性 → 看「系统类型」。

**方法 C：用 Keil 自带的驱动（不用下载）**

> ⚠️ **Keil MDK 5.37 之后的版本不再自带 ST-Link 驱动**。老版本才自带。

去 Keil 安装目录找：

```
C:\Keil_v5\ARM\STLink\USBDriver\
```

- **能找到** `stlink_winusb.inf` → 设备管理器 → 右键设备 → 更新驱动程序 → **浏览我的电脑上的驱动程序** → 指向这个文件夹 → 下一步
- **找不到这个目录** → 说明你的 Keil 版本不自带，回到方法 B

#### 第三步：验证

设备管理器里应该出现（**没有感叹号**）：

```
通用串行总线设备
  └─ STMicroelectronics STLink dongle
```

再到 Keil 里验证一次：魔术棒（Alt+F7）→ **Debug** → 选 `ST-Link Debugger` → **Settings** → 右侧 **SW Device** 列表出现下面这行：

```
IDCODE        Device Name
0x1BA01477    ARM CoreSight SW-DP
```

**看到这行就彻底通了。**

#### 第四步：驱动相关的常见坑

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 装完还是感叹号，属性里显示**代码 10 / 代码 43** | 供电不足或接口问题 | 换**主板后置 USB 口**，别用前面板，别用 USB Hub |
| 重启后驱动又消失了 | 克隆版 USB 描述符不规范 | 换一根**能传数据**的 USB 线（很多线只供电） |
| 设备名不是 STLink，VID 不是 `0483` | 极便宜的克隆版用了非 ST 芯片 | 官方驱动装不上，用商家给的驱动，或直接换一个 |
| Keil 提示 `ST-Link firmware upgrade required` | 固件太旧 | ⚠️ **先别升级**，见下面的警告 |
| Settings 打不开 / 报错 | 驱动和 Keil 不匹配 | 卸载驱动重装一次，重启电脑 |

> ⚠️ **关于固件升级的重要提醒**
>
> Keil 有时会提示 ST-Link 固件太旧要升级。**克隆版 ST-Link 升级固件有变砖风险**，升坏了就报废。
>
> - **只要还能正常下载程序，就别理这个提示**
> - 实在连不上必须升，用 ST 官方的 `ST-LinkUpgrade.exe`（在 STSW-LINK007 包里），**中途绝对不能拔线**
> - 原厂 ST-Link 随便升，没风险

---

## 二、接线

这一步是整个实验**最容易出错**的地方，慢慢来。

### 2.1 认识 ST-Link V2

你手上的下载器长这样：一个铝合金 USB 小棒，一头是 USB 公头，另一头是 **10 针的排针**（2 排 × 5 针），外壳上印了引脚名称。

外壳上通常印着这几个字：**`3.3V`、`SWDIO`、`SWCLK`、`GND`**（还有 `SWIM`、`RST` 等，我们不用）。

> ⚠️ 不同厂家的丝印顺序可能不一样，**一定照着你手上那个外壳印的字来对**，别照搬别人图片。

### 2.2 认识 Blue Pill 的下载口

核心板**顶部**（USB 口对侧）有一排 **4 针**排针，旁边的丝印标着：

```
┌───┬───┬───┬───┐
│3V3│GND│SWD│SWC│      ← 丝印常见写法
└───┴───┴───┴───┘
  ↑   ↑   ↑   ↑
 3.3V GND SWDIO SWCLK
```

如果丝印磨损看不清，也可以从**左侧排针**接，那里明确标了 `PA13`（= SWDIO）和 `PA14`（= SWCLK）。

### 2.3 推荐接法：只接 3 根线

| ST-Link | → | Blue Pill | 作用 |
| --- | --- | --- | --- |
| **GND** | → | **GND** | 共地，必须接 |
| **SWDIO** | → | **SWDIO / PA13** | 数据线 |
| **SWCLK** | → | **SWCLK / PA14** | 时钟线 |

**Blue Pill 的供电**：用一根 Micro-USB 线插到电脑 USB 口供电。

> 💡 **为什么不接 ST-Link 的 3.3V？**
> 因为板子已经用 USB 供电了。如果两路电源同时接，可能出现电压打架。只接 3 根（GND + SWDIO + SWCLK）是最稳的做法。
>
> 当然，如果你不想插 USB 线，也可以把 ST-Link 的 **3.3V** 接到板子的 **3V3** 上给板子供电——ST-Link 能输出约 100mA，Blue Pill 自己耗电很小，够用。但**两种方式只能选一种**，不要既插 USB 又接 ST-Link 的 3.3V。

接好后，**板子上那颗红色的电源灯（PWR）应该常亮**，说明供电正常。

### 2.4 检查 BOOT 跳线

板子底部有 **2 个跳线帽**（BOOT0 和 BOOT1）。

用 SWD 下载时，**两个跳线帽都必须插在 `0` 那一侧**（这是出厂默认位置）。

如果之前被人动过，要恢复成：BOOT0 = 0，BOOT1 = 0。

> 跳线帽一般插在标着 `0` 的两个针上。看着板子上丝印的 `0` 和 `1` 就知道。

---

## 三、搭 LED 电路

### 3.1 面包板的三条基本规则

搞懂这三条，面包板就随便插了：

1. **上下的两条长条**（通常一红一蓝）是**电源轨**，整条**从头到尾全连通**。红的一般接正极，蓝的接负极（GND）。
2. **中间有凹槽**，把板子分成左右两半。**凹槽左边 a~e 五个孔，同一行的这 5 个孔是连通的**；右边 f~j 同理。**凹槽左右两边不连通**。
3. 所以元件的两个脚要跨在**凹槽两侧**（比如一脚插 e 列、一脚插 f 列），才不会短路。

### 3.2 从核心板引电源到面包板

| 从 | 到 | 线色建议 |
| --- | --- | --- |
| Blue Pill 的 **3V3** 引脚 | 面包板**红色**电源轨 | 红 |
| Blue Pill 的 **GND** 引脚 | 面包板**蓝色**电源轨 | 黑 |

> Blue Pill 上 `3V3` 和 `GND` 都有好几个引脚，随便挑一个就行。
> ⚠️ 注意板子上还有 **5V** 引脚，**别接错**，LED 要用 3.3V。

### 3.3 三盏外接灯的接法（PA0 红灯为例）

**电气连接（记住这个就够）：**

```
PA0  ────  330Ω电阻  ────  LED正极(长脚)  ────  LED负极(短脚)  ────  GND
```

就是**电阻和 LED 串成一串**，一头接单片机引脚，另一头接 GND。

**面包板上怎么插：**

| 步骤 | 操作 |
| --- | --- |
| ① | 电阻**竖着**插：上脚插 `a5`，下脚插 `a8`（腿稍微掰一下，跨 3 行） |
| ② | LED **横跨中间凹槽**插在第 8 行：**长脚**插 `e8`，**短脚**插 `f8` |
| ③ | 跳线：Blue Pill 的 `PA0` → 面包板 `a5` |
| ④ | 跳线：面包板 `f8` → **蓝色电源轨（GND）** |

> 💡 **为什么只接两根线？** 因为 `a8` 和 `e8` 在**同一行左半区**，本来就连通。电阻下脚在 a8、LED 长脚在 e8，它们**自动连上了**，不用额外接线。这就是面包板规则 2 的用法。

**三盏灯的行号分配**（方法完全一样，只是换行号免得挤在一起）：

| 灯 | 单片机引脚 | 电阻上脚 | 电阻下脚 | LED 长脚 | LED 短脚 | 跳线接哪 |
| --- | --- | --- | --- | --- | --- | --- |
| 🔴 红灯 | **PA0** | `a5` | `a8` | `e8` | `f8` | f8 → 蓝轨 |
| 🟢 绿灯 | **PB0** | `a11` | `a14` | `e14` | `f14` | f14 → 蓝轨 |
| 🟡 黄灯（任务 3 才需要） | **PA1** | `a17` | `a20` | `e20` | `f20` | f20 → 蓝轨 |

一共 6 根跳线：

```
PA0 → a5        f8  → 蓝轨(GND)
PB0 → a11       f14 → 蓝轨(GND)
PA1 → a17       f20 → 蓝轨(GND)
```

> 黄灯是**任务 3** 才需要的。**先只接红灯、绿灯两盏跑通**，成功了再加第三盏。

### 3.4 LED 和电阻的识别

**LED 引脚**：

- **长脚 = 正极（阳极）**，接单片机引脚那一侧
- **短脚 = 负极（阴极）**，接 GND 那一侧
- 拿不准就翻过来看：LED 外壳上有一侧是**平的**，**平的那一侧是负极**

**电阻色环**：

| 阻值 | 色环（从一端读） |
| --- | --- |
| **330Ω** | 橙 - 橙 - 棕 - 金 |
| **1kΩ** | 棕 - 黑 - 红 - 金 |

> 电阻**没有正负极**，随便哪头都行。

### 3.5 蓝灯（PC13）怎么接 —— 注意方向反了！

**PC13 是三盏灯里唯一的例外。** 它只能"吸电流"、不能"送电流"（详见实验报告第三节），所以**接法跟另外两盏完全相反**：

```
3.3V  ────  LED正极(长脚)  ────  LED负极(短脚)  ────  1kΩ电阻  ────  PC13
```

| 步骤 | 操作 |
| --- | --- |
| ① | LED 跨凹槽插在**第 14 行**：**长脚**插 `e14`，**短脚**插 `f14` |
| ② | 1kΩ 电阻**竖着**插在**右半区**：上脚 `g14`，下脚 `g17` |
| ③ | 跳线：面包板 **`a14`** → **红色电源轨（3.3V）** |
| ④ | 跳线：面包板 **`i17`** → Blue Pill 的 **PC13** |

**为什么这样只接两根线：**

```
        左半区           凹槽          右半区
  行14   ● ● ● ● ●  ┃  ● ● ● ● ●
          ↑      ↑      ↑
        a14    e14    f14  g14
          │      │      └───┬───┘
      (跳线③)   长脚     同一组(f~j)，自动连通
          │      │          │
        3.3V   [LED]     [1kΩ电阻]      ← 电阻从 g14 竖跨到 g17
                  │          │
                 短脚        g17 ─┬─ i17 ─(跳线④)─ PC13
                                 └─ 同一组，自动连通
```

- 跳线③把左半区第 14 行接到 **3.3V**，**长脚 `e14`** 在这一组里 → 正极接 3.3V ✅
- **短脚 `f14`** 和电阻**上脚 `g14`** 是同一组（f~j 连通）→ **自动连上** ✅
- 电阻**下脚 `g17`** 和 `i17` 同一组 → 跳线④接到 **PC13** ✅

电流路径：`3.3V → LED → 电阻 → PC13`（符合 PC13 灌电流的要求）

**两个必须记住的点：**

1. **电阻要用 1kΩ**，不能用 330Ω。因为 PC13 最大只能吸 3mA，用 330Ω 会超。
2. **LED 正极要接 3.3V**，不能接 GND。接反了灯永远不亮。

> 💡 **嫌麻烦的话，蓝灯直接跳过不用接** —— 板子上 PC13 那个位置本来就已经焊了一颗 LED，程序跑起来它会自动亮，现象一样看得到。**建议第一次先不接，跑通了再补。**

---

## 四、Keil 建工程（核心步骤）

### 4.1 新建工程

1. 打开 Keil，菜单 **Project → New uVision Project...**
2. 选一个**全英文路径**保存，工程名填 `LED`
3. 弹出器件选择框，按顺序展开：
   `STMicroelectronics` → `STM32F1 Series` → `STM32F103` → 选 **`STM32F103C8`**
4. 点 **OK**

### 4.2 添加启动文件

上一步点 OK 后，会弹出 **Manage Run-Time Environment** 对话框（一个带勾选框的表格）。

在 **Device** 那一行，勾选：

- ☑ **Startup**（这一项会自动加入 `startup_stm32f10x_md.s` 启动文件，**必须要**）

在 **CMSIS** 那一行，勾选：

- ☑ **CORE**

> **其它全部不要勾**。我们写的是纯寄存器代码，不需要 STM32 标准外设库（StdPeriph Drivers），勾了反而会让工程变得臃肿。

点 **OK**。左侧工程树里应该出现 `Target 1 → Device → startup_stm32f10x_md.s`。

### 4.3 添加自己的代码

1. 左侧工程树里，右键 **`Source Group 1`** → **Add New Item to Group 'Source Group 1'...**
2. 左边选 **C File (.c)**，右边 Name 填 `led3`
3. 点 **Add**
4. 把 `led3.c` 的代码**整个粘进去**（替换掉里面的模板内容）

### 4.4 ⚠️ 关键一步：补上 SystemInit

启动文件里会调用一个叫 `SystemInit()` 的函数。如果你没加官方的 `system_stm32f10x.c`，编译时会报错：

```
Error: L6218E: Undefined symbol SystemInit (referred from startup_stm32f10x_md.o).
```

**解决办法：在 `led3.c` 的最下面，自己写一个空函数：**

```c
/* 启动文件会调用 SystemInit()，这里给个空实现
   不配置时钟，芯片就运行在复位后的默认状态：HSI 内部 8MHz */
void SystemInit(void)
{
}
```

**同时**，把代码里的主频常量改成 8MHz：

```c
#define SYSCLK_HZ   8000000UL   /* HSI 内部时钟 8MHz */
```

> **为什么要这么做？**
> STM32F103 上电复位后默认跑 **内部 8MHz HSI**，不需要外部晶振，**百分之百能跑起来**，不依赖任何库文件，也不用管板子上的 8MHz 晶振是好是坏。对流水灯这种应用，8MHz 完全够用，而且延时时间照样精确。
>
> 如果你后面想用 72MHz，就得加上官方的 `system_stm32f10x.c`，把 `SYSCLK_HZ` 改回 `72000000UL`。**第一次跑通先别折腾这个。**

### 4.5 配置魔术棒（Options for Target）

点工具栏上的**魔术棒图标**（或者菜单 `Project → Options for Target`，快捷键 `Alt + F7`）。

#### ① Target 选项卡

| 项目 | 设置 |
| --- | --- |
| Xtal (MHz) | `8.0`（这个只影响软件仿真，不影响实际硬件） |
| Use MicroLIB | ☑ 勾上（能减小代码体积，可选） |

#### ② Output 选项卡

- ☑ 勾选 **Create HEX File**（可选，以后想用串口下载会用到）

#### ③ Debug 选项卡 ← **重点**

1. 右上角的下拉框，从 `Use Simulator` 改成 **`ST-Link Debugger`**
2. 点右边那个 **Settings** 按钮，弹出新窗口：

**Debug 子页：**

| 项目 | 设置 |
| --- | --- |
| Port | 选 **SW**（不要选 JTAG） |
| Max Clock | 先保持默认；如果连不上就**调到 1MHz** |
| Reset | 选 `Autodetect` |

这个页面的右侧有个 **SW Device** 列表。如果接线正常，这里会显示一行：

```
IDCODE    Device Name        Move
0x1BA01477  ARM CoreSight SW-DP
```

**看到这一行，说明电脑已经认到芯片了，可以往下走了。**

如果这里是**空白**的，说明接线有问题，回到第二节检查。

**Flash Download 子页：**

- ☑ 勾选 **Reset and Run**（这样下载完程序自动运行，不用手动按复位键）
- 看 **Programming Algorithm** 列表里有没有 `STM32F10x Med-density Flash`（C8T6 是 64KB，属于中容量）
  - 有 → 不用管
  - 没有 → 点 **Add**，选 `STM32F10x Med-density Flash` 添加进来

3. 点 **OK** 关掉 Settings，再点 **OK** 关掉魔术棒

> 💡 补一句：如果 Settings 里那个 SW Device 列表**点开就报错**，八成是 ST-Link 的固件太旧。用 ST 官方的 `ST-LinkUpgrade.exe` 升级一下固件即可。

### 4.6 编译

按 **F7**（或点工具栏的 Build 图标，那个像小方块的）。

下方 **Build Output** 窗口最后一行显示：

```
0 Error(s), 0 Warning(s).
```

就说明编译成功了。有 Error 的话看第五节排错。

### 4.7 下载 / 烧录

板子插好 USB、ST-Link 插好电脑，然后按 **F8**（或点 Download 图标，那个带向下箭头的）。

下方显示：

```
Programming Done.
Verify OK.
```

**下载成功。** 因为之前勾了 `Reset and Run`，程序会自动开始跑，你应该立刻看到灯在闪了。

> 如果没自动跑，按一下板子上那颗 **RESET 按键**（在 USB 口旁边）手动复位。

---

## 五、排错速查表

### 5.1 编译期报错

| 报错信息 | 原因 | 解决 |
| --- | --- | --- |
| `Undefined symbol SystemInit` | 缺 system_stm32f10x.c | 自己写个空的 `void SystemInit(void){}` |
| `Undefined symbol ...` 别的符号 | 代码没粘全 | 检查是不是漏了函数 |
| `cannot open source input file "xxx.h"` | 引了不存在的头文件 | 我们的代码不需要任何头文件 |
| `expects a ";"` 之类 | 中文标点 | 检查 `；`、`，`、`（` 这些全角符号 |

### 5.2 下载期报错

| 报错信息 | 原因 | 解决 |
| --- | --- | --- |
| `No ST-LINK detected` | 驱动没装 / USB 没插好 | 装 ST-Link 驱动；换个 USB 口 |
| `No target connected` | 板子没供电 / 线接错 | 看 PWR 红灯亮没亮；检查 SWDIO、SWCLK 有没有接反（**先交换这两根试**） |
| `Cannot access target` | BOOT0 跳线不对 | 把 BOOT0 跳线帽插回 `0` 侧 |
| `RDDI-DAP Error` | SWD 时钟太快 | Debug → Settings → Max Clock 调到 1MHz |
| SW Device 列表空白 | 线的问题 | 重点查 GND 有没有接、杜邦线是不是断的 |
| `Flash Download failed` | 没选对 Flash 算法 | Settings → Flash Download → Add → 选 `STM32F10x Med-density Flash` |
| **`Cannot enter Debug Mode`** | **见下面 5.2.1 专节** | **分两种故障，别混淆** |

#### 5.2.1 专门讲 `Cannot enter Debug Mode`

先分清两种故障，处理思路完全不同：

| 现象 | 含义 | 问题出在哪 |
| --- | --- | --- |
| SW Device 列表**空白** | ST-Link 没认到芯片 | 接线 / 供电 |
| **`Cannot enter Debug Mode`** | ST-Link **认到芯片了**，但**控制不住内核** | SWD 被占用 / 复位时序 / 速度 |

报这个错说明**驱动是好的、线至少通了一部分**，按下面顺序排查：

**① 交换 SWDIO 和 SWCLK（30 秒，最常见）**

Blue Pill 顶部 4 针的丝印顺序各厂家不一样，照抄别人的图很容易接反。**先把这两根对调试一次。**

**② 确认供电和共地**

- PWR 红灯亮着吗？不亮 = 没供电
- GND 接了吗？不接地连不上
- 杜邦线插实了吗？拔下来重插

**③ 把 SWD 速度降下来**

Debug → Settings → **Max Clock** 改成 **1MHz 甚至 500kHz**。克隆版 ST-Link 配长杜邦线，高速率经常握手失败。

**④ 改成"复位时连接"（最对症的一招）**

Debug → Settings → **Debug 子页** → 找 **Reset** 下拉框：

- 从 `Autodetect` / `Normal` 改成 **`HW RESET`**
- 如果有 **`Connect under Reset`** 勾选框，**勾上**

**⑤ 按住 RESET 键手动抢时序（最有效，必试）**

1. **按住**板子上的 **RESET** 小按钮（USB 口旁边），**别松手**
2. 在 Keil 里点 **Settings**（或直接按 F8）
3. 在 ST-Link 提示"正在连接"的**那一瞬间松开 RESET**

原理：芯片刚复位的那几十毫秒里，用户程序还没跑、SWD 口还没被占用，ST-Link 能趁这个窗口抢到控制权。**节奏要练几次**，时机稍微提前或推后都行。

**⑥ 检查 BOOT0 跳线**

底部两个跳线帽，**BOOT0 必须在 `0` 侧**。

**⑦ 用 STM32CubeProgrammer 全片擦除**

如果芯片里已经烧过程序、把 PA13/PA14 配成了普通 GPIO，SWD 就被锁死了，Keil 很难抢回来。用 ST 官方的 **STM32CubeProgrammer**（免费）：

1. 连接方式选 **ST-LINK**
2. 点 Connect 旁边的箭头 → 选 **`Under reset`** 模式 → Connect
3. 连上后 → **Erasing & Programming** → **Full chip erase**

擦完回 Keil 就能正常连了。老工具 **STM32 ST-LINK Utility** 也行。

**⑧ 判断是 ST-Link 坏还是板子坏**

- 换 ST-Link → 好了 = 原来的 ST-Link 有问题（克隆版质量参差，很常见）
- 换 Blue Pill → 好了 = 板子有问题
- 换 USB 线 → 有些线只供电不传数据
- 换 USB 口 → 一定用**主板后置口**，别用前面板、别用 USB Hub

> **快速判断**：新板子第一次烧录，八成卡在 ① 或 ③；烧过一次程序之后才连不上，八成是 ⑤ 或 ⑦。

### 5.3 灯不亮（程序跑起来了但没现象）

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| **三盏灯全都不亮** | 忘记使能 GPIO 时钟 | 检查 `RCC_APB2ENR |= (1<<2)\|(1<<3)\|(1<<4);` 有没有写 |
| 某盏灯不亮 | LED 接反了 | LED 长脚要朝向单片机引脚那一侧 |
| 某盏灯不亮 | 杜邦线没插实 | 拔下来重插一次 |
| PC13 灯和程序预期相反 | PC13 是**低电平点亮** | 正常现象，见报告第三节 |
| PC13 外接灯怎么都不亮 | LED 正极接错到 GND 了 | PC13 的 LED 正极必须接 **3.3V** |
| 灯亮了但**不闪** | 程序卡死或没跑 | 检查是不是漏了 `while(1)` |
| 闪烁**特别慢**（约 3 秒一次） | 主频判断错了 | 把 `SYSCLK_HZ` 从 `8000000UL` 改成 `72000000UL`（或反过来） |
| 闪烁**特别快** | 同上 | 同上，反着改 |

---

## 六、验证连接是否成功的最快方法

如果怀疑接线有问题，不用急着写代码，先做这件事：

1. Keil 里随便建个能编译过的工程
2. 魔术棒 → Debug → 选 ST-Link → **Settings**
3. 看 **SW Device** 那一栏

**能显示 `ARM CoreSight SW-DP` 就说明接线百分百没问题。** 这是判断"是硬件问题还是软件问题"的分水岭。

---

## 七、完整操作清单（可以打印出来对着做）

```
□ 1. 装 Keil MDK + STM32F1 DFP + ST-Link 驱动
□ 2. 设备管理器里能看到 STLink dongle
□ 3. ST-Link 三根线接到板子：GND / SWDIO / SWCLK
□ 4. 板子插 USB，PWR 红灯亮
□ 5. BOOT0、BOOT1 跳线帽都在 "0" 侧
□ 6. 面包板引 3V3 和 GND
□ 7. 三盏 LED + 330Ω 电阻按第三节接好
□ 8. Keil 新建工程，器件选 STM32F103C8
□ 9. RTE 里勾 CMSIS→CORE、Device→Startup
□ 10. 新建 led3.c，粘贴代码
□ 11. 加上空的 SystemInit()，SYSCLK_HZ 改成 8000000UL
□ 12. 魔术棒 → Debug → ST-Link → Settings → SW Device 能看到 IDCODE
□ 13. Flash Download 勾 Reset and Run
□ 14. F7 编译，0 Error
□ 15. F8 下载，Programming Done
□ 16. 看灯 ✅
```

---

## 八、任务 3 怎么做

任务 3 只是在任务 2 的基础上**多接一盏黄灯（PA1）**，然后把代码从 `led3.c` 换成 `led4.c`。

操作上：

1. 面包板上再照第三节 3.3 的方法接一盏灯到 **PA1**（330Ω 电阻）
2. Keil 里把 `led3.c` 从工程中移除（右键 → Remove File），或者直接在原文件里替换代码
3. 新建 `led4.c` 粘进去（**同样要加空的 `SystemInit()`**）
4. F7 编译 → F8 下载

现象应该是：**红 → 绿 → 黄 → 板载蓝**，依次点亮，每盏 1 秒。
